<!-- {{#each books as |book|}}
  {{log book}}
  <section data-id="{{book.id}}">
    <h6>Title:</h6>
    <h3>
      {{limit book.title 15}}
    </h3>
    <h6>Author:</h6>
    <p>
      {{#if book.author}}
        {{book.author}}
      {{else}}
        No author provided.
      {{/if}}
    </p>
    <button class='btn btn-danger delete-button' data-id="{{book.id}}">DELETE</button>

  </section>

{{/each}} -->
<form class='sign-up' class='border'>
  <fieldset>
    <legend>Sign up</legend>
    <div>(do not use real password and email, this is NOT secure!!!)</div>
    <input type="text" name="credentials[email]" placeholder="Email">
    <input type="password" name="credentials[password]" placeholder="Password">
    <input type="password" name="credentials[password_confirmation]" placeholder = "Confirm Password">
    <input type="submit" value ="Sign up">
  </fieldset>
</form>

<form class='sign-in' class='border'>
  <fieldset>
    <legend>Sign in</legend>
    <input type="text" name="credentials[email]" placeholder="Email">
    <input type="password" name="credentials[password]" placeholder="Password">
    <input type="submit" value ="Sign in">
  </fieldset>
</form>
